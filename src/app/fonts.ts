import { Inter, Anton } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  weight: ["600"],
});

export const anton = Anton({
  subsets: ["latin"],
  display: "swap",
  weight: ["400"],
});
